<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
 <mapper namespace="com.gammza.chupachups.member.model.dao.MemberDao">
 	<resultMap type="member" id="memberMap"></resultMap>
 	
 	<insert id="insertMember">
	  insert into member(user_id, user_pwd, name, phone, location, email, create_at, update_at, status, point, temperature, kakao_idkey, naver_idkey, birthday, latitude, longitude) 
	  values ( 
	    #{userId},
	    #{userPwd},
	    #{name},
	    #{phone},
	    1,
	    #{email},
	    default,
	    #{updateAt},
	    default,
	    default,
	    default,
	    null,
	    null,
	    #{birthday},
	    null,
	    null
	  )
	</insert>

	<insert id="insertKakaoMember">
		insert into member(user_id, user_pwd, name, phone, email, create_at, status, point, temperature, kakao_idkey)
		values(
		'kakao',
		'kakao',
		#{name},
		'kakao',
		'kakao',
		default,
		default,
		default,
		default,
		#{kakaoIdkey}
	)
	</insert>	
 	
 	
 	
 	<!-- 아이디 중복체크 -->
	<select id="checkIdFunc" resultMap ="memberMap">
		select * from member 
		where user_id = #{userId}
	</select>

	<!-- <select id="selectOneMember" resultMap="memberMap">
 		select * from member
 		where user_id = #{userId}
 	</select> --> 	

 	<select id="selectOneMember" resultMap="memberMap">
 		SELECT * FROM MEMBER
 		join location on(location = location_no)
 		 where user_id = #{userId}
 	</select>
 </mapper>